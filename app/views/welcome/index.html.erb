<% unless current_user %>
  <%= button_to "Sign Up to Be a User", new_user_path, method: :get %>
  <%= button_to "I already have an account", login_path, method: :get %>
  <img src='<%= @gif_url %>'>

<% else %>
  <%= button_to "Log Out", logout_path, method: :get %>
<% end %>
